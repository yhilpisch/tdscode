# Primer Code Assets

The Data Scientist — Book 0 · Python Primer for Data Science  
(c) Dr. Yves J. Hilpisch  
AI-Powered by GPT 5.x

Lightweight scripts that mirror the primer chapters:

- `01_paths_and_artifacts.py` — create a first text artifact with `pathlib`.
- `02_first_plot.py` — generate a simple sine plot to `figures/primer_sine.png`.

Run from the book root:

```bash
python code/01_paths_and_artifacts.py
python code/02_first_plot.py
```

Outputs stay inside the book (`artifacts/` and `figures/`).
